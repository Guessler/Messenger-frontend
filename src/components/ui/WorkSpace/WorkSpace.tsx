
import React from 'react';
import { Box } from '@mui/material';

const WorkSpace = () => {
    return (
        <Box sx={{ display: 'flex', width: "100%",height: '96vh',  borderRadius: "12px",backgroundColor: '#FFFFFF', }}>

        </Box>
    );
};

export default WorkSpace;