
import React from 'react';
import { Box } from '@mui/material';

const Contacts = () => {


    return (
        <Box sx={{ display: 'flex', width: "300px",height: '96vh',  borderRadius: "12px",backgroundColor: '#FFFFFF', }}>

        </Box>
    );
};

export default Contacts;